# Contributing

## 写作目标

让开发者从"会用"走向"懂为什么这样用"。

每篇文档聚焦一个核心问题：**它是什么、为什么这样设计、怎样用才安全可靠高效。**
围绕这条主线展开，不发散、不堆砌，让读者读完后对这个工具建立清晰的心智模型，而不是记住一堆 API 和配置项。

## 写作准绳

### 1. 原理优先，用法为辅

不是"怎么用"的文档，而是"为什么这样工作"的文档。用法是原理的自然推论，讲清楚机制，用法读者自己就能推出来。

- 先讲内部机制、数据结构、执行流程
- 再讲 API / 配置项 / 使用方式
- 避免功能清单式的平铺罗列

### 2. 问题驱动，不平铺直叙

每篇文档围绕"开发者会遇到什么问题"来组织，而不是围绕"这个东西有什么功能"来罗列。功能清单是文档的骨架，问题才是文档的灵魂。

- 标题和章节用问题或场景命名，而不是功能名
- 每个知识点都要回答：这解决了什么问题？不用会怎样？
- 给出"为什么这样做"和"为什么不那样做"的对比

### 3. 单篇聚焦，不越界

每篇文档只解决一个核心问题。如果需要发散到其他主题，用链接引导，而不是在当前文档里展开。保持每篇文章的认知负担可控。

- 一篇文档 = 一个核心主题
- 相关但非核心的内容，链接到其他文档
- 控制单篇篇幅，过长时拆分为系列文章

## 文档结构

每篇文档建议遵循以下结构（根据主题灵活调整）：

1. **这篇文档解决什么问题** — 一句话说清楚，让读者决定是否需要读
2. **核心机制** — 底层原理、数据结构、执行流程
3. **关键概念** — 必须理解的概念，建立心智模型
4. **常见问题与陷阱** — 开发者实际会踩的坑，以及为什么会产生
5. **实践建议** — 安全、可靠、高效的使用方式
6. **延伸阅读** — 链接到相关主题，不在此展开

## 提交流程

1. 从 `dev` 分支创建特性分支
2. 编写或修改文档
3. 本地预览确认格式正确
4. 提交 PR 到 `dev` 分支
5. Review 通过后合并

## 提交规范

- 一个提交只做一件事
- 提交信息格式：`类型: 简要描述`
- 类型：`docs`（文档）、`fix`（修正）、`feat`（新内容）、`refactor`（重构）
- 示例：`docs: 新增 Kafka 分区策略深度解析`
